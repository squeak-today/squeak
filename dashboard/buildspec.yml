version: 0.2

phases:
  install:
    commands:
      - cd dashboard
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  baseDirectory: dashboard/build
  files:
    - '**/*'