Initialize once with `terraform init`
Create with `terraform apply`
Destroy with `terraform destroy`.


The current frontend has no way to authenticate with the email code,
so it needs to be done manually on AWS. A client-side solution should be added.