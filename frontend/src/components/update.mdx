import test_image from './update_images/img.png' 
import { Header, PText, Button, CenterContent, SmallImage, FullImage } from './UpdateComponents'

# Squeak Update Title
---

We can add images like this:
<SmallImage image_src={test_image}/>
<FullImage image_src={test_image}/>

<CenterContent>
  <PText>We can center images and content like this.</PText>
  <SmallImage image_src={test_image}/>
</CenterContent>

We can **bold**. We can *italicize*. We could also add components like the following:

<Button>We can even add buttons!</Button>
